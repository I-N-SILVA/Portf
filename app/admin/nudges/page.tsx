import { Placeholder } from "@/components/os/Placeholder";
export const metadata = { title: "Nudges — Shaft OS Admin" };
export default function Page() {
  return (
    <Placeholder
      eyebrow="admin · nudges"
      title="Nudges"
      note="Build re-engagement rules without code, and see every transmission that fired and whether it worked."
      phase="Phase 5"
    />
  );
}

import { Placeholder } from "@/components/os/Placeholder";
export const metadata = { title: "Billing — Shaft OS" };
export default function Page() {
  return (
    <Placeholder
      eyebrow="portal · billing"
      title="Billing"
      note="Your plan, invoice history and payment method — all in one place."
      phase="Phase 3"
    />
  );
}

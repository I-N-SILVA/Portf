import { Placeholder } from "@/components/os/Placeholder";
export const metadata = { title: "Messages — Shaft OS" };
export default function Page() {
  return (
    <Placeholder
      eyebrow="portal · messages"
      title="Messages"
      note="A direct line to the team — live, with file attachments."
      phase="Phase 6"
    />
  );
}

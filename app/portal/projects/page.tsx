import { Placeholder } from "@/components/os/Placeholder";
export const metadata = { title: "Projects — Shaft OS" };
export default function Page() {
  return (
    <Placeholder
      eyebrow="portal · projects"
      title="Projects"
      note="Track every project, its milestones and files — and sign off work when it's ready for you."
      phase="Phase 2"
    />
  );
}
